# 9) Faça um Programa que leia um vetor de 10 números reais e mostre-os na ordem inversa.

# Inicialize uma lista para armazenar os números
numeros = []

# Loop para ler os 10 números
for i in range(10):
    numero = float(input(f"Digite o {i + 1}º número: "))
    numeros.append(numero)

# Inverta a ordem dos números na lista
numeros_invertidos = list(reversed(numeros))

# Exiba os números na ordem inversa
print("Números na ordem inversa:")
for numero in numeros_invertidos:
    print(numero)
